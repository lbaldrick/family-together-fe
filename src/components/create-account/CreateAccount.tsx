import React, {ReactElement} from 'react'

export const CreateAccountContainer = (): ReactElement => {
    return <CreateAccountViewer />
};

const CreateAccountViewer = (): ReactElement => {
    return <div className={'login'} />
};
